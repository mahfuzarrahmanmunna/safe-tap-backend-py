# Example .env file for Firebase credentials
# Recommended: use FIREBASE_CREDENTIALS_BASE64 to avoid issues with newlines.

# Method A: Base64-encoded JSON
# Generate base64: python -c "import base64; print(base64.b64encode(open('safeTap/api/firebase-credentials.json','rb').read()).decode())"
# Then add:
# FIREBASE_CREDENTIALS_BASE64=<paste_base64_string_here>

# Method B: Raw JSON string (must escape quotes/newlines)
# FIREBASE_CREDENTIALS_JSON='{"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n..."}'

# Optional: you can also set individual keys (fallback)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_PRIVATE_KEY='-----BEGIN PRIVATE KEY-----\n...'
# FIREBASE_CLIENT_EMAIL=...@...gserviceaccount.com
```